# The stats SQLite database and its WAL/SHM sidecars are local runtime state,
# never committed. Keep this directory in git (so the default DB path exists)
# but ignore everything inside it.
*
!.gitignore
